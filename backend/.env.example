# Django
SECRET_KEY=replace-with-a-long-random-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:5173

# PostgreSQL
DB_NAME=plan_db
DB_USER=plan
DB_PASSWORD=replace-with-a-local-database-password
DB_HOST=127.0.0.1
DB_PORT=5432

# M-Pesa Daraja
DARAJA_ENVIRONMENT=sandbox
DARAJA_CONSUMER=replace-with-daraja-consumer-key
DARAJA_SECRET=replace-with-daraja-consumer-secret
DARAJA_PASSKEY=replace-with-daraja-passkey
DARAJA_SHORTCODE=replace-with-shortcode

# Leave disabled until callback handling (Task 9) is deployed and verified.
PAYMENT_INITIATION_ENABLED=False
MPESA_CALLBACK_URL=
MPESA_CONNECT_TIMEOUT=5
MPESA_READ_TIMEOUT=20

# Scoped guest access and purchased-download token lifetimes (seconds).
CHECKOUT_SESSION_MAX_AGE=86400
DOWNLOAD_TOKEN_MAX_AGE=600
