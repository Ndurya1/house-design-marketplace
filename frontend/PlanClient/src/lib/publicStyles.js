// Shared presentation for public pages; dashboard controls keep their own sizing.
const action = 'min-h-12 rounded-full px-6 py-3 text-sm font-semibold whitespace-normal text-center w-full sm:w-auto transition-colors';

export const publicPrimaryAction = `${action} border border-transparent bg-primary text-white shadow-sm hover:bg-primary-hover`;
export const publicSecondaryAction = `${action} border border-slate-300 bg-white text-slate-900 shadow-sm hover:bg-slate-50 hover:text-slate-900`;
export const publicCard = 'rounded-2xl border border-slate-200 bg-white shadow-sm';
